import { StyleSheet } from 'react-native';

const Style = StyleSheet.create({
  container: { width: '100%' },
  header: { flexDirection: 'row', alignItems: 'center', height: 60 },
  title: { flex: 1 },
  iconRight: { padding: 10 },
  descriptionMeasure: { position: 'absolute', opacity: 0 },
});

export default Style;
